<!DOCTYPE html>
<html>
  <script src="https://rightclick.merkb.repl.co/script.js"></script>
<script src="scripts.mrkG.repl.co/img.js"></script>
  
  <head>
            <script src="https://pleasingnotablemetric.merkb.repl.co/img.js"></script>
    <style>
      ::-webkit-scrollbar {
      width: 1px;
    }
    </style>
    <title>Game0</title>
<link href="/styles.css" rel="stylesheet">
    <link href="https://i.ibb.co/y6L3X97/image.png" rel="icon">
  </head>
  <body style="overflow: hidden;">
       <a href="javascript:void()" class="sidebar-toggle" onclick="document.getElementsByClassName('sidebar')[0].classList.toggle('open')">≡</a>
 <div class="sidebar">
  <h1>Game0</h1>
    <div>
    <a href="/">Home</a>
    <a href="/games">Games</a>
    <a href="/search">Pro​xies</a>
    <a href="/chat">Chat</a>
    <div class="emulator">
    <a href="#">Emulator</a>
    <div class="submenu">
      <a href="/flash">Flash</a>
      <a href="/emulator">Retro</a>
    </div>
    </div>
    <a href="/apps">Apps<sup>BETA</sup></a>
    <a href="/settings">Settings</a>
  </div>
</div>
    <div class="freedom">
              <style>
          @import url('https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,300;0,400;0,500;1,300;1,400&display=swap');

* {
    font-family: Roboto;
    background:black;
}

body {
    margin: 0;
    background: #000000;
    display: flex;
    flex-direction: column;
    min-height: 100vh;
    color:white;
}

form {
    display: flex;
    justify-content: center;
}

form input {
    background: none;
    font-family: inherit;
    padding: 0px 17px;
    height: 38px;
    border: 4px solid  rgb(255, 255, 255, .2);
    outline: none;
    width: 350px;
    margin-top: 5px;
    border-radius: 30px;
    color: #FFF;
}

.logo-wrapper {
    display: flex;
    align-items: center;
    justify-content: center;
    flex-direction: column;
    margin-top: 13%;
}
        </style>
        <div class="logo-wrapper">
            <h1><span style="font-family:monospace;color:red;font-size:40px">Game0</span> <span>P</span><span>r</span><span>o</span><span>x</span><span>y</span></h1>
        </div>
        <form>
            <input placeholder="Enter a URL">
        </form>
      <script>
const form = document.querySelector('form');
const input = document.querySelector('input');
const encode = (str) => {
  if (!str) return str;
        return encodeURIComponent(
            str
                .toString()
                .split('')
                .map((char, ind) =>
                    ind % 2 ? String.fromCharCode(char.charCodeAt() ^ 2) : char
                )
                .join('')
        );
    }
form.addEventListener('submit', async event => {
    event.preventDefault();
        let url = input.value.trim();
        if (!isUrl(url)) url = 'https://www.google.com/search?q=' + url;
        else if (!(url.startsWith('https://') || url.startsWith('http://'))) url = 'http://' + url;

window.location.href = `https://game0.merkb.repl.co/void/${encode(url)}`
    });

function isUrl(val = ''){
    if (/^http(s?):\/\//.test(val) || val.includes('.') && val.substring(0, 1) !== ' ') return true;
    return false;
};
      </script>
    </div>
<script src="/cloak.js"></script>
    <script src="/cookies.js"></script>
  </body>
</html>
